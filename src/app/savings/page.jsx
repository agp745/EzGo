import { DisplayMap } from "../../components/map"

export default function Savings() {

    return (
        <main className="flex">
            <DisplayMap expandedSidebar={true}/>
        </main>
    )
}